---
name: davinci-resolve-edit
description: "DaVinci Resolve 21.1のMCPと必要な画面操作で、ローカル動画のカット・トリム・タイムリマップ・字幕追加・映像と音声のクロスフェードを編集する。Resolveで動画や既存タイムラインを編集する依頼に使う。"
---

# DaVinci Resolve 動画編集

ローカル素材をResolve内の編集可能なタイムラインとして仕上げる。基準はResolve Studio 21.1のネイティブMCP。実行中のバージョンとAPI定義を確認し、説明は日本語を基本とする。

## 接続と素材確認

1. `mcp__davinci_resolve__get_resolve_status` と `get_whats_new` を呼ぶ。同じセッションで取得済みの更新情報は再取得不要。接続不可なら接続方法を調べ、接続済みとは報告しない。
2. `search_scripting_api` と `get_scripting_api(types=[...])` で必要な引数を確かめる。21.1には `SetSpeed`、`AddTransition`、`SetFades` がある。旧版の「APIでは不可能」という情報を流用しない。
3. 読み取り例は [scripts/inspect_timeline.py](scripts/inspect_timeline.py)。このファイルの**内容を** `run_script(script=...)` に渡す。注入変数 `resolve` を使うので、通常のPythonプロセス用ではない。
4. 指定ファイル/フォルダの絶対パス、存在、読み込み可否、fps、尺、解像度、音声チャンネルを確認する。対象が不明ならローカルパスを質問する。ドライブ全体を探索しない。
5. プロジェクトとタイムラインを名前とIDで特定する。候補から判定できなければ対象を質問し、他プロジェクトを勝手に開かない。

## 編集を具体化する

素材範囲・順序・速度・字幕・フェード・出力先を指示と素材情報から決める。指定済み事項の再確認や一律の承認待ちは挟まない。内容に合わせた編集を任されていれば映像・音声を確認して判断する。

範囲は `[開始, 終了)` の半開区間で記録する。次を混同しない。

- ソース先頭からのフレーム、素材内タイムコード、タイムライン絶対フレーム。
- 素材fpsとタイムラインfps。`24000/1001` 等の正確なレートとDF/NDF。
- 編集前のソース時刻と、カット・リタイム後の字幕表示時刻。

作業メモに素材パス、ソース範囲、配置先、速度区間、音声の扱い、字幕の時刻基準、トランジション尺を残す。映像クロスディゾルブと音声クロスフェードは別に扱う。音声は原則として映像との同期を維持する。

## 作業先と操作経路

元動画を書き換えずに編集する。既存編集では特に指定がなければ `DuplicateTimeline` で作業コピーを作り、`SetCurrentTimeline` の成功と元/コピーのIDを確認する。新規編集には別名の空タイムラインを使う。既存タイムラインのfps・カラー管理をこの作業の都合で変更しない。

コピー名は元名に編集内容と日時を付けるなど、衝突しない名前を選ぶ。字幕は必要に応じて新規字幕トラックへ配置する。名前やトラック番号だけを決めるために確認待ちにしない。

| 操作 | 優先経路 | 注意点 |
|---|---|---|
| 新規カット・トリム | `ImportMedia` → 範囲指定の `AppendToTimeline` | イン・アウトと実際の尺を再取得 |
| 既存クリップの分割・トリム | ResolveのEditページ | 削除・再挿入ではグレード/Fusion等を失い得る |
| 一定速度・静止 | `SetSpeed` → `GetSpeed` | リップル、リンク音声、ピッチ、最終尺 |
| 可変速度・滑らかなランプ | Retime Controls / Retime CurveのUI | 一定速度の分割だけでは滑らかなランプにならない |
| 音声から自動字幕 | `CreateSubtitlesFromAudio` | 文字と表示時刻の確認・修正 |
| 指定テキスト・SRT字幕 | SRT作成 → UIで読み込み・配置 | ファイル作成だけを字幕追加完了としない |
| 映像クロスディゾルブ | `AddTransition` | 両側の余裕フレーム、映像混合、尺 |
| 音声クロスフェード | `AddTransition(category="audio")` または重なる音声の `SetFades` | 隣接する端を無音にするだけではクロスフェードにならない |

APIの引数と例は [references/api-editing.md](references/api-editing.md) を読む。画面操作、可変速度、手入力字幕が必要な場合だけ [references/ui-editing.md](references/ui-editing.md) を読む。

画面操作には利用可能な `computer-use:computer-use` スキルを読み、そのツールを使う。使えなければMCPで可能な準備を進め、必要なUI操作を具体的に伝える。未対応操作を実行済みにしたり、無断で別エディターに置き換えたりしない。

純粋なResolve操作は `run_script`、ファイル調査やSRT生成は通常のローカルツールを使う。`run_script_unsafe` はResolve操作とOSアクセスを同一スクリプトで行う必要がある場合に限定する。

## 検証と納品

各段階で戻り値と変更後の状態を確認する。失敗・タイムアウト時は既存のクリップ・字幕・トランジション・ジョブを読み取り、同じ処理を重複実行しない。原因を特定できない変更を繰り返さず、実行済み範囲と次の手段を報告する。

- カット境界・終端、隙間・意図しない重複、音声リンクと同期を確認する。
- 速度箇所の尺、ランプ、音声の速度とピッチを再生で確認する。
- 字幕のフォント、誤字、改行、開始・終了、画面端からの余白を確認する。
- クロスフェード中央の映像混合と音のつながりを再生で確認する。API成功だけでは見た目や音の品質は確定しない。

`GetProjectManager().SaveProject()` の成功を確認する。出力も依頼されていればコーデック、出力先、字幕の焼き込み/別ファイル等を設定し、**今回追加したジョブだけ**実行する。詳細はAPI資料の「書き出し」。他のジョブを削除・開始しない。

最後にプロジェクト/タイムライン、編集内容、保存先、実際の検証範囲、残作業を簡潔に伝える。書き出し完了は `Complete` と出力ファイル確認後に報告する。
